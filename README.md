# elixir.land

landing page for elixir.land. single static `index.html`, hosted on GitHub Pages.

## local preview

```
open index.html
# or
python3 -m http.server 8000
```

## deploy

pushed to `main`, served by GitHub Pages at the `elixir.land` domain (see `CNAME`).
